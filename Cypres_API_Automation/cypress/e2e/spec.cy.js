
// Test suite
describe('empty spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')   
  })
})

//introducing a change to spec file
//lets commit from GIT


// lets commit from VS